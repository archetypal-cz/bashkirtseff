# Frontend A11y Wave — WS-B through WS-G (2026-07-03)

**Trigger:** KRR asked to execute the Codex-reviewed accessibility plan (docs/A11Y_PLAN.md) workstream by workstream, "same flow" each time: implement → verify live → Codex review → fold → commit → push. (WS-A ran 2026-07-02; this report covers the 2026-07-03 continuation plus the parallel about-pages content refresh.)

## What shipped (commits)
- `4578614af` about/home status refresh in 4 locales (parallel Fable agent) — redaction-phase story, "help us improve" flow (login → highlight → send notice), fixed the false "GitHub is private" claim
- `cce19f1f0` WS-B semantics/landmarks/headings (skip link, entry h1 guarantee, contentinfo, nav labels ×15 keys ×4 locales, aria-current)
- `ad21404b0` WS-C keyboard & focus (useDialog composable, all live overlays as real dialogs, listbox keyboard, native select, focus ring; **deleted dead FilterPanel.vue + BookSidebar.vue**)
- `61463ceb7` WS-D/E/F/H (flip aria-hidden+inert+lang, aria-live everywhere, footnote-popover dialog, 24px targets, reduced motion, a11y-audit script + `just a11y` + PR-blocking CI gate + vitest)
- `b09043d76` WS-G PWA specifics (theme_color tracking, OfflineStatus naming, admin h1)

**Outcome:** measured baseline 10–22 axe violations/page → **0 serious/critical across 64 scans** (8 pages × light,dark × 4 brands, full WCAG 2.2 AA ruleset), enforced by CI. Remaining: WS-I (human screen-reader audit).

## Agent lifecycle
- 5 agents spawned (about-pages content, 3 Codex reviewers, +1 from late 2026-07-02): **all delivered final reports unprompted** — zero chases, vs 7/13 the previous day.
- Codex review rounds: WS-B (1 real defect — year-page h3 siblings), WS-C (2 dead components + pin-transition focus bug + CSS duplication from a botched edit + FilterPanel double-Escape), WS-D/E/F/H (clean on all 6 flagged risk areas, incl. an empirical puppeteer repro disproving a suspected focus/scroll race; 2 minor notes folded). WS-G shipped gate-verified without a Codex round (20-line pattern-repeat diff; deviation noted in the commit).
- Verification tooling: raw puppeteer-core scripts ran ~10 browser sessions flawlessly (agent-browser CLI, which wedged repeatedly on 2026-07-02, was not needed).
- The axe gate itself caught one regression mid-development (lang-current chip 2.6:1 in dark after the WS-A token change).

## Teamcouch Review

**Reviewed**: 2026-07-03 · **Evidence**: today's 6 commits + 5 Codex review rounds across 2026-07-02..03 (no pipeline run reports — frontend session)

### Patterns Identified
- **External review finds what implementer + gates miss**: 4 of 5 Codex rounds found real defects → **frontend-dev SKILL updated** (pitfall: external review pass on substantial changes)
- **Twin-page sibling misses**: 2× in one week ([year]↔[carnet] index templates), both caught only by review → SKILL pitfall + WATCHLIST watch
- **Dead components documented as live**: FilterPanel + BookSidebar cost real work before discovery → SKILL pitfall (verify imports first) + WATCHLIST watch
- **Idle-without-report**: zero recurrence today (5/5 reported unprompted; non-editor roles, weak signal) — noted on the existing WATCHLIST item
- **puppeteer-core > agent-browser for scripted verification**: addendum on the existing daemon-degradation watch

### Skill Updates Applied
- `frontend-dev/SKILL.md`: three pitfalls added (twin pages, verify-imports, external review) with evidence comment

### WATCHLIST Changes
- Added: twin-page sibling misses (watch); docs-list-dead-components (watch)
- Annotated: idle-without-report (no recurrence 2026-07-03), agent-browser degradation (puppeteer-core addendum)
- Resolved: none moved (fixes too fresh — need 3+ clean reports)

### Recommendations for Human
- WS-I (manual NVDA/VoiceOver audit) is the remaining a11y item — needs a human ear
- The `sidebar-pinned` localStorage key + BaseLayout pre-paint class are now legacy of the deleted BookSidebar — harmless, candidate for cleanup in a future tidy pass
- about-pages agent flagged: marie.astro's "105 notebooks" (physical count) vs our 107 carnets; vision/AI-method sections still read mid-build — your call whether to rewrite
