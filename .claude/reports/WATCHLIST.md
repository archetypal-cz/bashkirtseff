# Watchlist

Known patterns and recurring issues to monitor during team runs. Teamcouch reads this to know what to look for in reports.

## Active Issues

### Agent Lifecycle

- [ ] **Context exhaustion mid-carnet** — agents silently stop producing output, no error message. Context compaction fails. Most common with large carnets (35+ entries) or agents doing double-duty (reading originals + translations). **Oversized-single-entry variant (uk-056-061)**: carnet 060's 1876-05-19 was a *single* 363-paragraph entry (whole carnet 884 paras); tr-060 finished 7/9 entries then stalled with zero progress on it and went idle without replying. Mitigation that worked: retire the stalled agent, spawn a FRESH-context replacement for just the remaining entry(ies), and instruct it to translate+SAVE in ~30-paragraph batches (never hold the whole entry in memory). Watch (1 instance) — on a 2nd, add "incremental batch-save for entries >~150 paras" to the translator skill.
- [ ] **Agents that don't stop** — after completing their carnet, some agents check TaskList for more work instead of stopping. Wastes resources and can cause conflicts.
- [ ] **"interrupted" idle states** — agents go idle with reason "interrupted" without clear cause. May indicate context limit during external API call (e.g., Gemini). Needs investigation.
- [ ] **Agents going off-rails** — occasionally an agent misinterprets its task or starts doing work outside its scope. Usually recoverable with a message, but wastes time.
- [ ] **Shutdown acknowledgment delays** — agents sometimes take multiple idle cycles before responding to shutdown requests. Not harmful but noisy.
- [ ] **Translators finish files but don't finalize task** — translator writes all files and sets `translation_complete: true` but goes idle WITHOUT marking the TR task `completed` or sending a summary, forcing the lead to chase them. 4/6 translators in uk-036-041; related to "Agent message delivery unreliable" (2026-05-24 x2). **Fix applied 2026-05-30**: translator skill now makes finalize (mark complete + summarize) an explicit step distinct from setting `translation_complete`; ED skill adds "watch disk state, nudge" guidance. Monitor for recurrence.
- [ ] **Review-agent idle poll-loops** — RED/CON spawned as teammates wake/check TaskList/re-idle every ~20-30s while their work is blocked, producing churn. 2 reports (uk-031-035, uk-036-041). **Fix applied 2026-05-30**: ED skill now instructs spawning review agents with "wait for ping, don't poll" and pinging per carnet. Monitor.
- [ ] **Critic subagent unavailable in team mode** — translator skill Phase 3 Pass 1 says to spawn an Opus grammar/naturalness critic via the Task tool, but teammates spawned via the Agent tool have no Task tool, so it's impossible in team mode (worked in non-team mode, e.g. uk-006-008). 2 team-mode reports (uk-031-035, uk-036-041); translators fell back to manual self-review at 0.95-0.96 quality. **Fix applied 2026-05-30**: translator skill clarified — team teammates do the self-review pass manually. Monitor.
- [ ] **Session-pause ping loss** — across a long session pause/resume, an in-flight team message (a CON ping for carnet 032) was dropped; the task sat pending with all entries editor-approved. 1 instance (uk-031-035). Caught by disk-state check. ED skill now says verify on disk after interruption. Watch for recurrence.
- [ ] **Conductor subagent type immediate idle/death** — conductor spawned as `conductor` subagent type went idle immediately without doing work (con), or partially completed then died mid-task marking it done prematurely (con-2). Workaround: spawn as `general-purpose`. Needs confirmation — 1 report only (2026-03-05-en-036-041).
- [ ] **Write tool path tracking bug** — when a translator agent attempts to `Read` a file that doesn't exist yet, the Write tool permanently blocks that path for the entire agent session. Workaround: pre-create empty placeholder files (`touch content/{lang}/{carnet}/*.md`) before spawning translators. Confirmed in 3 reports (en-065-070, en-093-106, en-091-103). ED skill should document this as standard practice.
- [x] **Concurrent edits to the same carnet (CONFIRMED 3 instances → ED + translator SKILLs updated 2026-06-10)** — two agents editing one carnet at once. Now **3 reports**: cz-050-055 (translator post-convention cleanup pass vs RED review), cz-065-069 (con2 spawned onto 069 before con acked it was off → ~16-entry overlap), uk-072-074 (tr-a woke from idle on a "self-audit your carnet" instruction and re-edited all 34 files of 072 while editor red-2 was reviewing it). **No data loss in any** (line-level `Edit`s coexisted) — but luck, not design, each time. **Fix applied 2026-06-10**: ED skill now has a "one writer per carnet — serialize" rule (shut the translator down / send `shutdown_request` at the RED handoff; confirm a 1st reviewer is off a carnet before spawning a 2nd); translator skill step 6 now says self-audit is pre-finalize only and bars post-handoff re-edits (message the lead instead). Monitor for recurrence. Reports: 2026-05-31-cz-050-055.md, 2026-06-07-cz-065-069.md, 2026-06-10-uk-072-074.md.
- [ ] **ED/reviewer acting on an agent claim that ground truth contradicts** — agents assert things (a cross-carnet term "collision", which carnet is the style outlier, a "still incomplete" list) that disk/source verification disproves; acting on the claim risks a wrong "fix". uk-050-055: ED nearly normalized «Сорока» (Marie's *authorial reuse* of a magpie pet-word for different men) as if it were translator drift — red2 verified against source and reverted. cz-050-055: RED+CON both said 051 was the em-dash outlier (grep proved 053); tr-4's stale completeness claim contradicted by git. **Verify claims against source/disk before acting on them.** **3 reports** — cz-056-064: red-a asserted `just verify-carnet` "doesn't exist" (it does — justfile line 454, runs PASS); the lead nearly logged it as a defect before checking the justfile. **ED skill updated 2026-06-07** (verify mechanical claims — tool/recipe existence, file state — against the justfile/disk before acting). **4th instance (cz-065-069):** red-b reported 069 had reached RED 36/36 when disk showed 10/36 (off by 26) — verified before acting, no wrong action taken. The discipline keeps paying off. Reports: 2026-05-30-uk-050-055.md, 2026-05-31-cz-050-055.md, 2026-06-07-cz-056-064.md, 2026-06-07-cz-065-069.md.

- [ ] **API-overload mid-run freeze (recoverable)** — a ~55-min Anthropic overload (uk-056-061, ~09:20–10:14) froze all active teammates (CON, RED, a translator) with processes alive but idle; no error, no termination. **Detection**: disk-file mtimes (nothing written for ~50 min) — NOT idle notifications (idle is normal between turns and gives no signal). **Recovery that worked, zero data loss**: send a resume/wake-up ping with precise current-state instructions to each frozen agent; live-but-idle processes resume on the ping. Only respawn an agent if a ping yields no new disk write within a few minutes. Committed flags/edits up to the freeze were intact. Watch (1 instance) — codify the recovery play in the ED skill on a 2nd. Report: 2026-06-06-uk-056-061.md.
- [ ] **Drift-grep must cover all spelling variants** — checking for terminology drift with a single-variant grep misses other misspellings. uk-056-061: a grep for «Піетро» (і-е) reported carnets "clean" while «Пієтро» (і-є) persisted (RED found 31 in carnet 060); a third variant «Пьетро» also exists. Lesson: grep ALL plausible variants of a name before declaring it normalized, and keep the canonical TM line listing the known wrong forms. Watch (1 instance). Report: 2026-06-06-uk-056-061.md.
- [x] **Translator destroys structural scaffolding while writing (CONFIRMED → translator SKILL updated 2026-06-06)** — meta-pattern: translators mechanically break invisible structure that reads fine but only a mechanical check catches. Now **2 reports / 3 instances**: cz-050-055 (tr-5/tr-6 glossary path-depth → 1,515 broken links, missed by RED+CON) is the SAME defect as uk-062-064 (b) tr-064 path-depth drift mid-carnet (608 broken links; first 4 entries correct); plus uk-062-064 (a) tr-063 used `Write` and **stripped YAML frontmatter** from 11 of 14 files. The path-depth defect cleared the 2nd-occurrence threshold, so **a "Structural Integrity — Do Not Break the Scaffolding" section was added to the translator SKILL** (canonical `../../_original/_glossary/` path on every entry, no mid-carnet drift; preserve frontmatter when overwriting, prefer Edit over Write for existing files; run `just check-links` + `just check-frontmatter`/`verify-carnet` before finalizing). The frontmatter-strip kind is folded into the same self-check. Backstop in progress: `just verify-carnet` mechanical gate (team-lead). **Now watch whether the skill edit + gate eliminate recurrence.** Reports: 2026-05-31-cz-050-055.md, 2026-06-06-uk-062-064.md. (Supersedes the old "Translator copies source glossary paths without depth adjustment" watch under Glossary Research — same defect.)
- [ ] **Persistent-reviewer fatigue on long waves (NEW, watch)** — uk-062-064: RED was excellent on carnets 1–2 (063/064) but **dipped on carnet 3/3 (062, the largest, ~0.88)**, missing substantive mistranslations and a systematic Collignon name/gender error that CON then caught. The TR→RED→CON defense-in-depth (independent CON verification) was the backstop — nothing reached final approval uncorrected. Consider rotating/refreshing RED after ~2 large carnets. Watch (1 instance). Report: 2026-06-06-uk-062-064.md.
- [ ] **Review-agent idle ~24 min after assignment (queued-message non-activation)** — cz-056-064: con sat idle ~24 min after the 059 CON assignment; it only engaged when a follow-up status-check ping arrived. A queued task assignment did not activate the agent on its own. **Mitigation**: after assigning a carnet to a review agent, verify engagement on disk (conductor/editor count climbing) within a few minutes; a plain status ping reliably kicks a stalled review agent into gear. Related to "Session-pause ping loss". 1 instance. Report: 2026-06-07-cz-056-064.md.
- [ ] **Stale self-addressed `task_assignment` echoes after completion** — cz-056-064: completed tasks generated stale `task_assignment` echo messages with `assignedBy` = the agent itself (con #19/#20/#21; translators #7/#8). Harmless; agents correctly no-op after the first. Harness quirk — note so leads/agents don't chase them. 1 instance. Report: 2026-06-07-cz-056-064.md.
- [ ] **Missing `conductor_approved` frontmatter field (tr-scaffold gap)** — cz-056-064: carnets 063, 064, 062 had no `conductor_approved` field on any entry (056 did); CON added it per entry. Systematic scaffold gap across 3 carnets — worth a one-time upstream fix so new translation files include the field. Report: 2026-06-07-cz-056-064.md.

### GEM Corruption Patterns

- [ ] **Inline GEM comments splitting paragraph text** — Gemini places `%% GEM: ... %%` mid-paragraph ~50% of the time, despite explicit prompt instructions. Currently caught by manual audit or RED review.
- [ ] **Gemini translating French in comments** — Gemini occasionally "translates" the French original text inside `%% ... %%` comments, corrupting the source reference.
- [ ] **Duplicate paragraph insertion** — Gemini copies a paragraph block, resulting in duplicate IDs and text.
- [ ] **Removed markup** — Gemini strips `==highlight==` markup on code-switches or removes footnote references.

### Translation Quality

- [ ] **Gallicism rates** — track frequency of gallicisms caught by GEM/RED/OPS per carnet. High rates may indicate translator needs updated guidance.
- [ ] **False friend frequency** — especially for Czech (ceremonie, kostým, kabinet) and English (sympathetic, actually, revolt/révolter, eve/veille, vilain/naughty). New EN false friends found in 008-010 run (2026-02-16).
- [ ] **Quality score trends** — CON scores should trend upward as skills improve. Czech: 0.937 avg (016-021) → 0.970 avg (022-027). Quality jump attributed to RED as general-purpose (direct fixes) and polarity check addition. English: stable at 0.95-0.96 range — plateau reached. Ukrainian: 0.94 → 0.947 → 0.952 → 0.953 → 0.952 → 0.96 → 0.956 (w7, 031-035) → 0.963 (w8, 036-041) → ~0.942 (050-055) → ~0.949 (056-061) → ~0.923 (062-064) avg — stable plateau ~0.92-0.96; 062-064 (0.92/0.93/0.92) slightly lower, attributable to RED fatigue on the largest carnet (CON-backstopped) rather than translation quality.
- [ ] **Orphaned footnotes** — translators define footnote definitions (`[^xxx]: ...`) but forget the in-text superscript marker (`[^xxx]`). 6 instances across 4 carnets and 2 of 3 translators in 042-047 run. tr-002 had zero in carnet 047. Caught by RED. Proposal: add footnote-linking self-check to translator skill. Reports: 2026-03-05-en-042-047. Needs confirmation — 1 report only.
- [ ] **Code-switch misidentification** — EN translators highlighted French idioms as English code-switches in 2 instances (009, 010). "It takes my breath away" and "that's all I'm saying!" were translated French idioms, not Marie writing in English. Needs confirmation — 1 report only (2026-02-16).
- [ ] **Footnote ID format inconsistency** — EN translators use two formats: `[^fn047-034]` (fn+carnet-seq) and `[^45.266.1]` (carnet.para.seq). Both valid markdown, but inconsistent across carnets. 125 occurrences of fn-format vs 58 of para-format. Consider standardizing. Reports: 2026-03-05-en-042-047, also observed across 001-003 and 036-047.
- [ ] **Conditional 1st/3rd-person reversal (CZ, `kdyby` vs `kdybych`)** — the dominant cz-056-064 error; several meaning-reversing (e.g. 063.0492 "S'il retournait à moi" inverted to make Marie the subject; 058.0062/0159; 062.0266/0314/0316; 059 cluster). French "si je…"→kdybych, "si vous…"→kdybyste/kdybys, "si il/elle/on…"→kdyby. **Mitigation that worked**: mid-wave broadcast to active translators + RED grep gate (`grep kdybych(om)? + object pronouns`, verify person vs French subject). Post-broadcast, tr-056 + tr-063 self-caught instances; carnets 060 and 062 came through with 0 reversals. **Propose**: add the conditional-person self-check to the translator skill and the grep to the editor checklist. Multiple instances, 1 wave. Report: 2026-06-07-cz-056-064.md.
- [ ] **Dropped-letter mechanical typos (CZ, OCR-adjacent)** — high-frequency single-letter drops/jams, esp. dropped "l" in "milovat" → "miuji/miuje" (**37 occurrences in carnet 056** alone, the most love-saturated carnet; scattered elsewhere), plus mid-word truncations/jammed finite verbs ("nemi lovala", "doumírala skončila") and a mid-word sentence cut (062.0324, restored). Caught by RED reading + greps. Propose a pre-RED "milovat" + adjacent-typo grep. 1 wave, many instances. Report: 2026-06-07-cz-056-064.md.
- [ ] **Cyrillic contamination incl. lookalike-letter swaps (CZ)** — confirms the cross-language Cyrillic pattern in Czech too: whole Cyrillic words (известно, 058 "bил") AND Cyrillic letters substituted inside Latin words — "Audiff**ета**" (Cyrillic ета for Latin eta) ×4, invisible to the eye, caught only by a Cyrillic scan. Mechanical Cyrillic scan is essential; don't false-positive intentional foreign-word citations in footnotes (e.g. Russian "(щенок)"). Report: 2026-06-07-cz-056-064.md.
- [ ] **Czech leaking into French `%%` reference comments (CZ)** — copy-paste artifact where Czech replaces words in the French source-reference comment, damaging source-of-truth (5 in 056, 3 in 062, also seen in 057/059/060/064). Restore the French. Recurs across the wave. Report: 2026-06-07-cz-056-064.md.

### Tooling / Process

- [x] **`just verify-carnet` adoption — RESOLVED, gate now run pre-RED** — the mechanical gate (justfile line 454, `src/scripts/verify-carnet.ts`; checks frontmatter, links, glossary-depth, footnotes, %%-balance, latin-in-cyr, foreign-script) went un-run in cz-056-064 (editors caught those defects by hand). **In cz-065-069 the ED ran it pre-RED on all 5 carnets** — it caught an orphaned footnote in 069/03-28 (mis-numbered definition) that would otherwise have slipped downstream, and PASS-gated every carnet before RED. Adoption confirmed valuable. Reports: 2026-06-07-cz-056-064.md, 2026-06-07-cz-065-069.md.
- [ ] **GATE GAP — `verify-carnet` does NOT catch duplicate (or missing) paragraph IDs** — cz-065-069: red-a found **6 duplicate paragraph IDs** in carnet 068 (a heading block sharing a content block's ID) that `verify-carnet` PASS-ed. **Propose: add a duplicate-paragraph-ID check (and missing-ID check) to `src/scripts/verify-carnet.ts`.** Until then, editors keep a manual dup-ID grep. Report: 2026-06-07-cz-065-069.md.
- [ ] **TOP RECOMMENDATION — TM-locked-name lint at the verify-carnet gate (proper-noun/transliteration drift)** — the dominant, recurring defect class across **3+ waves** (065-068, 069-071, 072-074) is *valid-Cyrillic* proper-noun drift: a TM-locked name appearing in a correct-looking but non-canonical spelling (Олександр/Алессандро, Башкирцева/Башкірцева, ніццянці/ніццяни, Ерістов/Еристов, Маркуар/Маркуаре, Денгоф/Дьонгоф). It is **invisible to `verify-carnet`** (every char is valid Cyrillic) and to a reading review; caught only by alert RED/CON + ED greps. **uk-072-074 was the extreme case**: "Alexandre" maps to 5 referents (Larderei→«Алессандро» / uncle→«Олександр» / Tsar→«Олександр II» / Dumas→«Александр Дюма» / Batourine→«Олександр»), and the glossary alias-tag is *unreliable* (auto-tags the bare name to one entity regardless of true referent — the per-paragraph RSR comment is the authoritative signal). **Propose (architect): a TM-locked-name lint** — flag a pinned name in a non-canonical spelling, with a per-carnet allow-list for legitimate homographs (saint/pope «Олександр», novelist «Александр Дюма»). **Skill-side mitigation already shipped** (conductor SKILL, 2026-06-10): CON hunts these collisions in its pre-read and hands the lead a broadcastable locked-names + traps list. But the mechanical lint is the real fix. Escalated to human/architect. Reports: 2026-06-07-uk-065-068.md(+069-071), 2026-06-10-uk-072-074.md.
- [ ] **GATE GAP — `verify-carnet` does NOT catch double-UTF-8 mojibake** — uk-072-074: CON found a double-encoded "Ремі" (rendered as the byte-run "Ð ÐµÐ¼Ñ…") at 073/07-20 that the `latin-in-cyr`/`foreign-script` checks PASS-ed (the bytes aren't single stray Latin letters in an otherwise-Cyrillic word — they're a contiguous mojibake run). **Propose: add a mojibake check to `src/scripts/verify-carnet.ts`** — flag contiguous `Ð`/`Ã`/`Ñ`/`Â` byte runs (`grep -P '[\xC2-\xC3\xD0-\xD1]{2,}'`-style). 1 instance; joins the dup-ID gate-gap as an accumulating set of verify-carnet enhancements for the architect. Report: 2026-06-10-uk-072-074.md.
- [ ] **Translator subagent has no shell → can't run `verify-carnet`** — cz-065-069: the `translator` subagent type lacks Bash, so the translator skill's "run `just verify-carnet` before finalizing" step is unexecutable in team mode. The **ED-run pre-RED gate covers it**; translators still do manual greps and self-caught conditionals. Same family as "critic subagent unavailable in team mode". Translator skill should note the gate is ED-run. Report: 2026-06-07-cz-065-069.md.
- [ ] **CON batch-sets `conductor_approved` at end of carnet (monitor via heartbeat, not disk-count)** — cz-065-069: con does the whole three-pass review then batch-flips all entries at the end, so `conductor_approved` disk-count stays 0/N during the read then jumps to N/N. Do NOT read 0/N as "stuck". **Protocol adopted this wave: CON sends a ~halfway heartbeat on 30+-entry carnets** — eliminated false-stall alarms. Codify in ED + conductor skills. Report: 2026-06-07-cz-065-069.md.
- [ ] **Parallelizing a review stage: confirm redirect ack BEFORE spawning the parallel agent** — cz-065-069: a 2nd conductor (con2) was spawned onto 069 before con had acknowledged its "do 066→067 only" redirect → both reviewed 069 simultaneously (~16-entry overlap). Caught via disk check; **zero data loss** (line-level edits coexisted; one duplicate APPROVE comment tidied). Same class as "Concurrent edits to the same carnet". **Lesson: when adding a 2nd editor/conductor to a carnet, confirm the existing agent has acked it's off that carnet before spawning.** Report: 2026-06-07-cz-065-069.md.
- [ ] **Mid-paragraph RED comment placement on long single-line paragraphs** — when an editor `Edit`s a fix into a long single-line paragraph, the inserted `%% RED %%` comment can land mid-paragraph (matching a sentence fragment), splitting readable text. Both editors hit it on cz-056-064; reliable end-scan `grep 'RED:.*%% [^%]'` then rejoin. **Propose**: add the end-scan to the editor skill's standard end-of-review pass. Report: 2026-06-07-cz-056-064.md.
- [ ] **Larger/scaled team validated for big-carnet waves** — cz-056-064 ran 8 agents (5 TR + 2 RED + 1 CON), ~0.92 avg, split-review on the one 35-entry carnet. cz-065-069 (three ~37-entry carnets) **scaled dynamically to up to 4 editors + 2 conductors**: base 2 editors, a **tail-split** (red-a tripwired at 30/37 on 067 → fresh red-d took the dense 7-entry tail), a **fresh-context relief editor** (red-c for 069), and a **2nd conductor** for parallel CON. Context tripwires prevented any wall (red-b went solo through the dense 37-entry 066, ~75 fixes, no wall). Held ~0.92. **Caveat:** the parallel 2nd conductor caused a brief 069 collision — see "Parallelizing a review stage: confirm redirect ack before spawning". Reports: 2026-06-07-cz-056-064.md, 2026-06-07-cz-065-069.md.

### Corpus Data Quality

- [ ] **Broken local Windows image paths in source (drawing markers)** — `file:///C:/Users/kerra/.../TomeN-NN.png` embedded where Marie drew sketches; render as broken images. Scope: **16 active `_original` entries** (carnets 017, 018, 019, 024, 025, 027, 028, 029, 059, 096, 098, 101) — already **propagated into translations** (cz 10, uk 9, en 7, fr 16 files). con converted the 059 instance to an editorial note + commented the dead path. Needs a deliberate corpus-wide pass: link to real scanned drawings / Kernberger images, or convert all to editorial notes. (The `_original/_archive/raw_carnets/*` hits are expected raw OCR, ignorable.) Found via CON on cz-056-064. Report: 2026-06-07-cz-056-064.md.

### Glossary Research

- [ ] **Paragraph ID granularity variance** — glossary research agents produce wildly different PID densities (16 PIDs/245 lines vs 70 PIDs/286 lines). Not a quality issue yet, but may cause confusion if PIDs are referenced elsewhere. Confirmed across 6 batches (50 entries). Not blocking. Reports: 2026-05-24-glossary-top15, 2026-05-25-glossary-research.
- [ ] **glossary-merge appends cruft** — the merge tool does "simple append" of the full source file content to the target, requiring manual cleanup after every merge. Consider adding a `--no-append` or `--clean` flag. 6 merges required manual cruft removal. Report: 2026-05-25-glossary-research.
- [ ] **GIOIA miscategorized as places/cities** — person entry (Amélie Gioia, demi-mondaine) filed under places/cities/ due to early auto-generation error. 222+ content links prevent easy move. Report: 2026-05-25-glossary-research.
- [ ] **Translator copies source glossary paths without depth adjustment** — translators copy `%% [#Tag](../_glossary/…) %%` tags verbatim from `content/_original/{carnet}/`, but from `content/{lang}/{carnet}/` the correct relative path is `../../_original/_glossary/…`. tr-5 (055) + tr-6 (050) did this → **1,515 broken links across 65 entries**. The defect is invisible to a reading review (link *text* looks fine) and **slipped past both RED and CON**; caught only by an ED link-resolution grep. tr-1–tr-4 got it right, so it's translator-dependent, not skill-systemic — but a mechanical check would catch it regardless. **Tooling added 2026-05-31**: `just check-links {lang} {carnet}` (and `just check-links-all {lang}`) flags any relative `.md` link whose target doesn't resolve from the file's location — run it before committing a translated carnet, and consider adding link-resolution to the RED checklist. **All known cz instances fixed 2026-05-31** (050, 055, 001, 009, 011, 095 — sed + check-links verified clean). Still a watch: translators may reintroduce it; check-links is the guard. **Now graduated into the translator SKILL** as part of "Translator destroys structural scaffolding while writing" (Agent Lifecycle section) after a 2nd occurrence in uk-064 (mid-carnet drift, 608 links) — see that entry. Report: 2026-05-31-cz-050-055.md.
- [ ] **Stale glossary paths in older carnets (cz 010/018/019 FIXED 2026-05-31)** — two legacy link styles broke after the glossary was reorganized into subcategories: (a) missing-subcategory paths (`people/ANITCHKOFF.md` → actual `people/recurring/ANITCHKOFF.md`; many also cross-category, e.g. `people/MILORADOVITCH.md` → `culture/literature/`), and (b) an older site-root style with bare TitleCase names (`/_original/_glossary/Dina.md` → `../../_original/_glossary/people/core/DINA.md`). **Fixed** by basename-resolution remap (find actual location, rewrite to relative path): cz 010, 018, 019 — 85 unique targets remapped. **fr + uk also swept 2026-05-31**: fr had ~54k depth-bug + 92 site-root links, uk ~2.4k depth-bug — all repaired (path-depth sed + site-root sed + basename remap of 238 subcategory/bare-name targets), via the new reusable scripts `just propagate-tag` and `just remap-glossary-links`. Path/style link rot is now eliminated across cz/uk/fr (en was already clean). A second pass then resolved targets that existed under **other names / subfolders** (basename + curated cross-name map, each verified): name variants (ITALIE→places/cities/ITALY, VIENNE→VIENNA, RUSSIE→RUSSIA, SWITZERLAND→SUISSE, CHAMBRE→CHAMBRE_DES_DEPUTES, CERCLE/MEDITERRANEE→CERCLE_DE_LA_MEDITERRANEE, NOTRE_DAME_DE_NICE→NOTRE_DAME_NICE, LA_GIOIA→GIOIA) and honorific/title forms (M_BARNOLA→BARNOLA, M_TORMOSOFF→TORMOSOFF, MARQUIS_DE_LAMBERTYE→LAMBERTYE, AUNT_NADINE→NADINE, etc.). en was also basename-remapped (118 links). **Final link health: cz 0 broken, fr 0 broken, en 158→40, uk 98→63.**
**Residual = genuinely-MISSING glossary entries** (content gaps, NOT path bugs): en ~30 + uk ~45 distinct — family/group stubs (HOWARD_CHILDREN, ANITCHKOFF_FAMILY, MICHEL_FAMILY, PATTON_FAMILY), individuals (COMTE_GRIMAU, MME_DE_BALLORE, M_TCHERNIKOFF, and compound first+last refs like LEON_BONNAT/FERDINAND_DE_LESSEPS that may exist under the surname), places (FOLIES_NICOISES, RUMPELMAYER_CAFE, VILLA_DURANDO), culture (APOLLO_BELVEDERE, MENDELSSOHN_CONCERTO). **Next: an RSR/entry-creation (or further surname-curation) pass** to clear these. Reports: 2026-05-31-cz-050-055.md.
- [x] **Source-side TODOs from uk-056-061 — RESOLVED 2026-06-06** (uk-062-064 side pass). (a) `FANNY_LEAR.md` stub → full researched entry (Harriet "Hattie" Blackford / Grand Duke **Nikolai Konstantinovich** / 1874 diamond scandal / 1875 memoir / Mirafiori–Larderei link), cited. (b) `059/1876-04-20` location fixed — and a **full carnet-059 location audit** corrected **11 entries** (clean Naples→Rome→Nice arc). Propagated to uk/en. Report: 2026-06-06-uk-062-064.md.
- [ ] **Location auto-extraction artifacts (CONFIRMED recurring)** — `location` frontmatter is frequently set to a city Marie only names *rhetorically* ("Paris ne peut se vanter… que Naples") or recalls in *past tense*, not where she physically is. 059 audit (2026-06-06) found 11 such errors; 056-061 had 04-20. **researcher SKILL updated** with a "location ≠ most-mentioned city" pitfall. Likely present in other travel-heavy carnets — candidate for a systematic source-side location audit. Reports: 2026-06-06-uk-062-064.md, 2026-06-06-uk-056-061.md.
- [ ] **Footnotes not written back to source (systemic, backfill planned)** — `_original/` has reader-facing `[^n]` footnotes for only 6 early carnets; from ~050 on each language authored its own independently and they diverge widely (EN richest — 052: en=143/uk=17; 063: en=73/uk=6). EN is the canonical-quality source. **Decision: full 050–064 backfill via a script (modeled on `propagate_glossary_tag.py`, EN→_original, additive, dry-run first), DEFERRED post-wave.** Plan in `footnote-backfill-plan` memory. Report: 2026-06-06-uk-062-064.md.
- [x] **Orphaned theme tag with no source basis (`[#Marriage]`/MARRIAGE.md)** — cz 018 (1874-04-10) had 2 `[#Marriage]` tags pointing to a non-existent MARRIAGE.md. **Resolved 2026-05-31**: authored a comprehensive `culture/themes/MARRIAGE.md` thematic entry (id MARRIAGE; scoped to marriage-as-institution to complement, not duplicate, LOVE.md). The cz links resolved automatically once the file existed; full `check-links-all cz` now clean. Follow-up (optional, deferred): the French *source* paragraphs aren't yet tagged `[#Marriage]` — adding source-side tags where marriage-as-institution is discussed would let other languages inherit them. Report: 2026-05-31-cz-050-055.md.

### Pipeline Efficiency

- [ ] **Gemini API rate limits** — track how often we hit quota. May need to space out GEM passes or batch differently.
- [ ] **Translator throughput variance** — some agents consistently faster than others. May indicate model or prompt differences.

### OPS vs GEM

- [ ] **OPS same-model blind spots** — theoretical concern: Opus reviewing Opus translations may share systematic blind spots. No evidence across 4 runs (264 entries), but keep monitoring. If 5 runs pass with no evidence, consider resolving. Reports: 2026-02-16-en-008-009-010.md, 2026-02-16-uk-006-008.md, 2026-02-17-uk-009-011.md, 2026-02-17-uk-012-014.md.

### Ukrainian-Specific

- [ ] **Dialogue-punctuation convention inconsistency (cross-language)** — translators choose different dialogue markers and it diverges per-carnet/per-translator because the translator skill states no explicit convention. UK: `---` (3 hyphens) vs `—` (em dash), uk-006-008. CZ: line-start em-dash (053, tr-4) vs Czech curly quotes „…" (the house style across 49 prior carnets + 5/6 of the batch), cz-050-055 — required a dedicated post-hoc conversion pass on 053. 2 reports / 2 languages. **On a 3rd instance, add an explicit per-language dialogue-punctuation convention to the translator skill** (CZ: „…" curly quotes for direct speech, never line-start em-dashes in body; speaker tags split the quote). Sub-note: in cz-050-055 both RED and CON *misidentified which carnet was the outlier* (said 051; grep proved it was 053) — trust a mechanical grep over agent recollection for consistency questions. Reports: 2026-02-16-uk-006-008.md, 2026-05-31-cz-050-055.md.
- [ ] **Fix rate trend (UK)** — declining across 8 waves: 0.42 → 0.28 → 0.22 → 0.31 → 0.27 → 0.14 → 0.08 (w7) → 0.05 (w8) /entry. **Rebounded in 056-061**: RED was substantially active again — 4 polarity reversals, Pietro normalization across TWO spelling variants, heavy Latin-contamination cleanup (esp. 061), cross-carnet name reconciliation. Driven by (a) two translators (056, 060) reading the TM before the canonical «П'єтро» line landed, and (b) a recurring per-translator Latin-letter contamination tendency — i.e. input-quality variance, not a review regression. Quality held (~0.949). Reports: all UK reports.
- [ ] **Cyrillic contamination (CONFIRMED recurring, cross-language, now mechanically catchable)** — Latin chars embedded in Cyrillic words ("Тумanchoфи", "Лорантi", "Граcій", "баронe", "моindре"), stray apostrophes ("Віскон'ті"), Russian ё/ы in Ukrainian, Chinese 历; **and the reverse in Czech** — Cyrillic letters inside Latin words ("Audiff**ета**" ×4) + whole Cyrillic words (известно). Now **4 reports across 2 languages** (2x 2026-05-24 + uk-056-061 + cz-056-064; see the CZ-specific item under Translation Quality). Model artifact, translator-introduced. RED catches it on review; the `just verify-carnet` `latin-in-cyr`/`foreign-script` checks now flag it mechanically — run the gate. Reports: 2026-06-06-uk-056-061.md, 2026-06-07-cz-056-064.md.
- [ ] **Fabricated words (cross-language)** — translators generate nonexistent words. Ukrainian: "збільшувально", "боянним", "спіла" (uk-018-022). Czech: "voitře" (cz-016-021). 3 reports across 2 languages. Translator skill updated with fabricated word warning (2026-05-24).
- [ ] **Agent message delivery unreliable** — agents complete tasks and mark them done, but completion messages don't reach team lead. Requires proactive task/file state checking. 2 instances (2026-05-24) + reinforced by uk-031-035/uk-036-041 (session-pause ping loss; translators idling pre-finalize). Infrastructure issue; mitigated by disk-state checking (now in ED skill). See also "Translators finish files but don't finalize task" and "Session-pause ping loss" above.
- [ ] **Code-switch visibility loss (UK)** — Marie's deliberate Cyrillic "мене" inside French text becomes invisible in Ukrainian translation. CON resolved with footnote in 024.0207. 1 report only (2026-05-24-uk-023-025). Document as standard practice if recurs.

## Resolved Issues

- [x] **`MUSIC_THEME.md` project-wide broken glossary link** — the thematic-tag entry was mis-named `MUSIC.md` while its `id` was already `MUSIC_THEME`, its paragraph IDs `GLO_MUSIC_THEME.NNNN`, and all 2,771 content references (+ the `[#Music_theme]` tag) pointed to `MUSIC_THEME.md`; **0 files** referenced `MUSIC.md`. **Resolved 2026-05-31** with a single `git mv MUSIC.md → MUSIC_THEME.md` (commit 4242daf4) — repaired every reference project-wide, no other edits. Verified resolving from `_original/` and translation dirs; `just glossary-missing` clean.
- [x] **OPS zero-corruption track record** — 3 runs (204 entries: EN 008-010 65 + UK 006-008 78 + UK 009-011 61), 0 corruption, 0 false positives across all. OPS is the preferred reviewer over GEM. GEM remains available for cross-model validation when desired. Resolved 2026-02-17.
- [x] **Russianisms checklist effectiveness** — 3 runs (199 entries), 0 overt russianisms from explicit checklist. Checklist baked into translator skill file (Language-Specific Guidance > Ukrainian). Resolved 2026-02-17.
- [x] **Subtle russianisms (calques)** — 3 runs. OPS 3-pass caught calques ("абсолютно", "факт у тому") and morphological russianisms (-ой endings) that the explicit checklist missed. Guidance baked into opus-editor skill file (Ukrainian section). Resolved 2026-02-17.
- [x] **3-pass OPS review value** — 3 UK runs confirmed. Fix rate: 0.42 → 0.28 → 0.22/entry. Each wave, OPS caught a distinct category the checklist missed (wave 1: false friends; wave 2: calques; wave 3: morphological). 3-pass review is proven valuable for Ukrainian. Resolved 2026-02-17.
- [x] **Conductor subagent type lacks Edit access** — 7+ reports (all UK waves, EN 006-014, EN 015-018, EN 019-021, EN 036-041). Conductor subagent type only has Read/Grep/Glob despite SKILL.md declaring Edit/Write. Fix: spawn CON as `general-purpose` subagent type. Proven since 2026-02-27 (en-015-018). ED skill updated to default to general-purpose. Resolved 2026-03-05.
- [x] **Editor subagent type has no Edit access** — Resolved 2026-05-24: Switch RED to `general-purpose` subagent type. Confirmed zero corruption across 150 entries (cz-022-027). ED skill updated.
- [x] **Agents being too chatty** — fixed by adding Agent Teams Protocol to all skills (idle behavior, when to message, when not to). Resolved 2026-02-12.
- [x] **Unnecessary RSR/LAN agents** — source preparation is complete for all 106 carnets. Stopped spawning RSR/LAN in translation pipeline. Resolved 2026-02-12.
- [x] **Wikipedia link validation needed** — link checker agent validated 32 URLs across 15 entries, fixed 7 broken links (wrong article names, non-existent pages, markdown truncation). Remaining ~250+ links are lower-risk standard articles. Resolved 2026-05-25.
- [x] **Diary quote accuracy in glossary entries** — agents consistently cited specific carnet/paragraph references; spot-checks across multiple entries show quotes are accurate transcriptions from diary files. Resolved 2026-05-25.
- [x] **Breslau city/person disambiguation** — 348 [#Breslau] tags retargeted from auto-generated city stub to LOUISE_BRESLAU (the painter). City stub and BRESLAU_LOUISE redirect deleted. Resolved 2026-05-25.
- [x] **Glossary deduplication (14 entries)** — MLLE_COLLIGNON, 3x GALITZINE, 3x JULIAN variants, 2x BASTIEN, M_JULIAN, 2x CASSAGNAC père, 2x CASSAGNAC refs all merged into canonical entries using `just glossary-merge`. ~1,400 content links updated across ~1,200 files. Resolved 2026-05-25.
- [x] **Per-carnet agent lifecycle** — adopted "one carnet = one agent" pattern to prevent context exhaustion. Works reliably. Resolved 2026-02-12.
- [x] **Self-review pass** — added three-phase translate (Think, Translate, Self-Review) to translator skill. Reduced gallicism rates. Resolved 2026-02-13.

## How to Update

- **New pattern observed once**: Add to Active Issues with a `[ ]` checkbox and "Needs confirmation" note
- **Pattern confirmed (3+ occurrences)**: Remove "Needs confirmation", add occurrence count
- **Pattern resolved**: Move to Resolved Issues with `[x]` checkbox and date
- **Teamcouch updates**: `/teamcouch` may propose additions/removals based on report analysis
